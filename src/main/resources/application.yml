server:
  port: 8081

telegram:
  bot:
    token: ${TELEGRAM_BOT_TOKEN}
  chat:
    id: ${TELEGRAM_CHAT_ID}

notification:
  telegram:
    enabled: true
  discord:
    enabled: false
    webhook-url: ${DISCORD_WEBHOOK_URL:}

logging:
  level:
    com.example.silverprice: INFO
